<div class="space-y-6">
  <div>
    <h1 class="text-2xl font-bold text-gray-800">Confirmer la livraison</h1>
    <p class="text-gray-600 mt-1">Commande n°<%= commande.idcommande %> — <%= commande.reference || 'Sans référence' %></p>
  </div>

  <% if (commande.statut === 'livree') { %>
    <div class="bg-green-50 border border-green-200 rounded-lg p-4">
      <p class="text-green-900 text-sm font-medium mb-1">Livraison enregistrée</p>
      <p class="text-green-800 text-sm">
        Cette commande est déjà marquée comme livrée. Retournez au tableau de bord pour voir le compteur mis à jour.
      </p>
    </div>
  <% } else { %>
    <div class="bg-amber-50 border border-amber-200 rounded-lg p-4">
      <p class="text-amber-900 text-sm font-medium mb-1">Étape finale</p>
      <p class="text-amber-800 text-sm">
        Vérifiez les articles ci-dessous, puis cliquez sur le bouton vert
        <strong>« Confirmer la livraison »</strong> en bas de page pour enregistrer la livraison.
      </p>
    </div>
  <% } %>

  <%- include('_lignes_commande_table') %>

  <div class="bg-white rounded-lg shadow p-6 flex flex-col sm:flex-row justify-between items-center gap-4">
    <a href="/fournisseur/commande/<%= commande.idcommande %>" class="px-4 py-2 border border-gray-300 rounded-lg text-gray-700 hover:bg-gray-50">
      <i class="fas fa-arrow-left mr-2"></i>Retour aux détails
    </a>
    <% if (commande.statut !== 'livree') { %>
      <form method="POST" action="/fournisseur/commande/<%= commande.idcommande %>/confirm-delivery"
        onsubmit="return confirm('Confirmer définitivement la livraison de cette commande ?');">
        <button type="submit" class="bg-green-600 text-white px-8 py-3 rounded-lg hover:bg-green-700 font-semibold text-lg shadow-md">
          <i class="fas fa-truck mr-2"></i>Confirmer la livraison
        </button>
      </form>
    <% } else { %>
      <a href="/fournisseur/dashboard" class="bg-emerald-600 text-white px-8 py-3 rounded-lg hover:bg-emerald-700 font-semibold text-lg shadow-md inline-block">
        <i class="fas fa-check-circle mr-2"></i>Retour au tableau de bord
      </a>
    <% } %>
  </div>
</div>
