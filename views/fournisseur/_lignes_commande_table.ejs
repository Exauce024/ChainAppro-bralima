<div class="bg-white rounded-lg shadow">
  <div class="p-6 border-b border-gray-200">
    <div class="flex items-center space-x-3">
      <div class="w-10 h-10 bg-purple-100 rounded-lg flex items-center justify-center">
        <i class="fas fa-box text-purple-600"></i>
      </div>
      <div>
        <h3 class="text-lg font-semibold text-gray-800">Articles commandés</h3>
        <p class="text-sm text-gray-500"><%= typeof lignes !== 'undefined' ? lignes.length : 0 %> article(s)</p>
      </div>
    </div>
  </div>

  <div class="overflow-x-auto">
    <table class="min-w-full divide-y divide-gray-200">
      <thead class="bg-gray-50">
        <tr>
          <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Matière première</th>
          <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Quantité</th>
          <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Prix unitaire</th>
          <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Total ligne</th>
        </tr>
      </thead>
      <tbody class="bg-white divide-y divide-gray-200">
        <% if (typeof lignes !== 'undefined' && lignes.length > 0) { %>
          <% lignes.forEach(function(ligne) { 
            var totalLigne = Number(ligne.qtecommande) * Number(ligne.prixunitaire);
          %>
            <tr>
              <td class="px-6 py-4 text-sm font-medium text-gray-900">
                <%= ligne.libelle %>
                <% if (ligne.description) { %>
                  <p class="text-xs text-gray-500 mt-1"><%= ligne.description %></p>
                <% } %>
              </td>
              <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-600"><%= ligne.qtecommande %> Kg</td>
              <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-600"><%= formatMoney(ligne.prixunitaire) %></td>
              <td class="px-6 py-4 whitespace-nowrap text-sm font-semibold text-gray-900"><%= formatMoney(totalLigne) %></td>
            </tr>
          <% }); %>
          <tr class="bg-gray-50">
            <td colspan="3" class="px-6 py-4 text-right text-sm font-semibold text-gray-700">Total commande</td>
            <td class="px-6 py-4 whitespace-nowrap text-sm font-bold text-gray-900">
              <%= formatMoney(commande.montant || 0) %>
            </td>
          </tr>
        <% } else { %>
          <tr>
            <td colspan="4" class="px-6 py-8 text-center text-sm text-gray-500">
              Aucun article enregistré pour cette commande.
            </td>
          </tr>
        <% } %>
      </tbody>
    </table>
  </div>
</div>
